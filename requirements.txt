# FastAPI & Server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# CORS & HTTP
httpx>=0.28.1
aiofiles>=23.2.1

# AI Services - Text
anthropic>=0.7.7          # Claude AI
openai>=2.0.0             # ChatGPT, DALL-E, TTS
google-genai>=1.51.0      # Google GenAI SDK for image generation (Nano Banana 2)
google-generativeai>=0.8.0  # Google Gemini API for audio transcription

# AI Services - Image
replicate>=0.22.0         # Image models (Flux, etc.)
Pillow>=10.1.0            # Image processing

# AI Services - Audio
elevenlabs>=0.2.26        # TTS & Music generation
pydub>=0.25.1             # Audio processing and manipulation
# pydub imports the stdlib `audioop`, which PEP 594 removed in Python 3.13.
# arkserver runs 3.11 and never noticed; pdrei runs 3.13 and has failed to
# start since at least 2026-08-05 with `ModuleNotFoundError: pyaudioop`,
# taking every release down with it. audioop-lts restores both module names.
audioop-lts>=0.2.1; python_version >= "3.13"

# Translation
deep-translator>=1.11.4    # Google Translate (free, no API key)

# Realtime grant verification (CloudV2 Voice-Companion, Post #1215)
# RS256/JWKS for auth-api capability JWTs. cryptography backs the RSA verify path.
PyJWT[crypto]>=2.8.0
cryptography>=41.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
PyPDF2>=3.0.1             # PDF processing for document analysis
certifi                   # TLS CA certificates
